interface:
  display_name: "Bounded Orchestrator"
  short_description: "Review and execute one assigned chunk or group"
  default_prompt: "Use $orchestrator for the assigned chunk or explicitly named group, preserving actual-diff review and independent validation; stop at that boundary."
